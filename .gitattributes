# Normalize line endings to LF in the repository regardless of the
# contributor's OS/editor, avoiding noisy cross-platform diffs.
* text=auto eol=lf

# Diff binary-ish assets sanely instead of as text
*.png binary
*.jpg binary
*.jpeg binary
*.svg text eol=lf
*.ico binary
